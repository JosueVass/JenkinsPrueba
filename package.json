{
  "name": "pizzeria",
  "version": "1.0.0",
  "description": "paquete de prueba",
  "scripts": {
    "api-test-production": "newman run --disable-unicode tests/pizzeriaV1.postman_collection.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "Josue",
  "dependencies": {
    "newman": "^5.2.2"
  }
}
